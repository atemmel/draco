#!/bin/cherry

import fs

output := draco

g++ ./src/main.cpp -o $output -lSDL3 -Iinclude

if (len $argv) == 0 {
    exit 0
}

if (get $argv 0) == run && (fs'exists $output) {
    #TODO: this currently fails
    #"./{output}"
    ./draco
}
